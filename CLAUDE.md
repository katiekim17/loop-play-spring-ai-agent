# CLAUDE.md

## 코드 작성 규칙

- 아키텍처 다이어그램 코드블록에 언어 태그를 명시해야 한다.
- 예외 흐름에서 로그가 누락되지 않도록 try/finally를 사용한다.
- 로깅 시 민감정보(고객 메시지, 주문번호 등) 노출 여부를 점검한다.
- 외부 호출 실패(LLM, API 등)는 @RestControllerAdvice로 잡아 사용자 친화적 메시지와 적절한 HTTP 상태 코드를 반환한다.
